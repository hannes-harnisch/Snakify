#include <filesystem>
#include <fstream>
#include <string_view>
#include <vector>

int main(int argc, char* argv[])
{
	using namespace std;
	using namespace std::filesystem;

	vector<string_view> v(argv, argv + argc);
}
